import envSetup from "@utils/env_setup";

envSetup.configure();

/**
 * @notice Todo
 */
contract("Multisig", async (accounts) => {
  describe("Proposing and voting on a tx", () => {
    it("...");
  });

  describe("Executing the result of a tx", () => {
    it("...");
  });
});
